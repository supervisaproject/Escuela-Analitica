#El objetivo de este ejercicio es crear una cadena de caracteres, un entero y un numero real. La variable de tipo cadena de caracteres debe contener la palabra
# "hola", la variable de tipo real debe contener el numero 10 y la variable de numero entero debe contener el numero 20
#si hemos completado el ejercicio de forma satisfactoria apareceran en la consola los tres valores

#The objective of this exercise is to create a string of characters, an integer and a real number. The character string variable must contain the word
# "hello", the real type variable must contain the number 10 and the integer variable must contain the number 20
#if we have completed the exercise satisfactorily, the three values ​​will appear on the console

# Write your own code here
# Escribe tu propio codigo aqui
micadena = None
mireal = None
mientero = None

'''
NO TOCAR !!!
DO NOT TOUCH !!!
'''
# probando el codigo
# testing the code
if micadena == "hola":
    print ("Cadena: " + micadena)
if isinstance(mireal, float) and mireal == 10.0:
    print ("Real: " + str(mireal))
if isinstance(mientero, int) and mientero == 20:
    print ("Entero: " + str(mientero))
